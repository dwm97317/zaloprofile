# 会员等级功能说明文档

## 概述

后端系统实现了完整的会员等级管理功能，支持等级创建、自动升级、折扣权益等核心能力。

---

## 数据库表结构

### 1. 会员等级表 `yoshop_user_grade`

| 字段名 | 类型 | 说明 |
|--------|------|------|
| grade_id | int | 等级ID (主键) |
| name | varchar | 等级名称 (如：大众会员、黄金会员、铂金会员、钻石会员) |
| weight | int | 等级权重 (1-50，数字越大等级越高) |
| upgrade | json | 升级条件 `{"expend_money": 1000}` |
| equity | json | 等级权益 `{"discount": 9.5}` |
| status | tinyint | 状态 (0=禁用, 1=启用) |
| is_delete | tinyint | 软删除标记 |
| wxapp_id | int | 小程序ID |
| create_time | int | 创建时间 |
| update_time | int | 更新时间 |

### 2. 等级变更记录表 `yoshop_user_grade_log`

| 字段名 | 类型 | 说明 |
|--------|------|------|
| log_id | int | 日志ID (主键) |
| user_id | int | 用户ID |
| old_grade_id | int | 原等级ID |
| new_grade_id | int | 新等级ID |
| change_type | int | 变更类型 (10=后台设置, 20=自动升级) |
| wxapp_id | int | 小程序ID |
| create_time | int | 创建时间 |

### 3. 用户表 `yoshop_user` 相关字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| grade_id | int | 当前会员等级ID |
| expend_money | decimal | 累计消费金额 (用于等级升级判断) |
| pay_money | decimal | 累计支付金额 |

---

## 核心业务逻辑

### 1. 等级升级条件

升级条件存储在 `upgrade` JSON 字段中：
```json
{
  "expend_money": 1000  // 累计消费满1000元自动升级
}
```

### 2. 等级权益

权益存储在 `equity` JSON 字段中：
```json
{
  "discount": 9.5  // 9.5折，10表示不折扣
}
```

### 3. 自动升级机制

系统通过定时任务 (`app\task\behavior\user\Grade`) 自动检测并升级用户等级：

1. 获取所有可用等级列表（按权重降序）
2. 遍历每个等级，查询满足升级条件的用户
3. 批量更新用户等级并记录变更日志

升级判断条件：
- 用户累计消费金额 >= 等级升级条件金额
- 用户当前等级权重 < 目标等级权重

### 4. 折扣价格计算

```php
// 折扣价格 = 原价 × (折扣率 / 10)
$discountPrice = $originalPrice * ($discountRatio / 10);
```

---

## API 接口

### 1. 获取用户信息（含等级）

**接口**: `GET /api/user/detail`

**响应示例**:
```json
{
  "code": 1,
  "msg": "success",
  "data": {
    "userInfo": {
      "user_id": 123,
      "nickName": "用户昵称",
      "grade_id": 2,
      "grade": {
        "grade_id": 2,
        "name": "黄金会员",
        "weight": 10,
        "upgrade": {
          "expend_money": 1000
        },
        "equity": {
          "discount": 9.5
        },
        "status": 1
      },
      "expend_money": "1500.00",
      "pay_money": "2000.00"
    }
  }
}
```

### 2. 获取等级列表

**接口**: `GET /api/user/grade/list` (需自行实现)

**建议响应**:
```json
{
  "code": 1,
  "msg": "success",
  "data": {
    "list": [
      {
        "grade_id": 1,
        "name": "普通会员",
        "weight": 1,
        "upgrade": {"expend_money": 0},
        "equity": {"discount": 10}
      },
      {
        "grade_id": 2,
        "name": "黄金会员",
        "weight": 10,
        "upgrade": {"expend_money": 1000},
        "equity": {"discount": 9.5}
      },
      {
        "grade_id": 3,
        "name": "铂金会员",
        "weight": 20,
        "upgrade": {"expend_money": 5000},
        "equity": {"discount": 9}
      },
      {
        "grade_id": 4,
        "name": "钻石会员",
        "weight": 30,
        "upgrade": {"expend_money": 10000},
        "equity": {"discount": 8.5}
      }
    ]
  }
}
```

---

## 前端对接指南

### 1. 用户等级展示

从 `/api/user/detail` 接口获取用户信息，`grade` 关联字段包含完整等级信息。

```typescript
interface UserGrade {
  grade_id: number;
  name: string;           // 等级名称
  weight: number;         // 等级权重
  upgrade: {
    expend_money: number; // 升级所需消费金额
  };
  equity: {
    discount: number;     // 折扣率 (0-10)
  };
  status: number;         // 状态
}

interface UserInfo {
  user_id: number;
  grade_id: number;
  grade: UserGrade | null;
  expend_money: string;   // 累计消费金额
  pay_money: string;      // 累计支付金额
}
```

### 2. 等级进度计算

```typescript
// 计算升级进度百分比
function calculateProgress(currentExpend: number, nextLevelRequirement: number): number {
  if (nextLevelRequirement <= 0) return 100;
  return Math.min((currentExpend / nextLevelRequirement) * 100, 100);
}

// 计算距离下一等级还需消费金额
function getAmountToNextLevel(currentExpend: number, nextLevelRequirement: number): number {
  return Math.max(nextLevelRequirement - currentExpend, 0);
}
```

### 3. 折扣价格计算

```typescript
// 计算折扣后价格
function getDiscountPrice(originalPrice: number, discountRate: number): number {
  // discountRate: 9.5 表示 9.5折
  return Number((originalPrice * (discountRate / 10)).toFixed(2));
}

// 示例
const price = 100;
const discount = 9.5;
const finalPrice = getDiscountPrice(price, discount); // 95.00
```

### 4. 等级变更类型

```typescript
enum GradeChangeType {
  ADMIN_USER = 10,    // 后台管理员设置
  AUTO_UPGRADE = 20,  // 系统自动升级
}
```

---

## 后台管理接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/store/user.grade/index` | GET | 等级列表 |
| `/store/user.grade/add` | POST | 添加等级 |
| `/store/user.grade/edit` | POST | 编辑等级 |
| `/store/user.grade/delete` | POST | 删除等级 |

---

## 相关文件路径

```
Lineminiapp/source/application/
├── common/
│   ├── model/user/
│   │   ├── Grade.php          # 等级基础模型
│   │   └── GradeLog.php       # 等级变更日志模型
│   └── enum/user/grade/log/
│       └── ChangeType.php     # 变更类型枚举
├── api/
│   ├── model/user/
│   │   ├── Grade.php          # API等级模型
│   │   └── GradeLog.php       # API日志模型
│   └── service/user/
│       └── Grade.php          # API等级服务
├── store/
│   ├── controller/user/
│   │   └── Grade.php          # 后台等级控制器
│   └── model/user/
│       └── Grade.php          # 后台等级模型
└── task/
    ├── behavior/user/
    │   └── Grade.php          # 自动升级任务
    └── model/
        ├── User.php           # 任务用户模型
        └── user/
            ├── Grade.php      # 任务等级模型
            └── GradeLog.php   # 任务日志模型
```

---

## 前端需要实现的功能

1. **会员中心页面**
   - 显示当前等级名称和图标
   - 显示累计消费金额
   - 显示升级进度条
   - 显示距离下一等级还需消费金额

2. **等级权益展示**
   - 显示当前等级折扣率
   - 显示各等级权益对比

3. **商品价格展示**
   - 原价展示
   - 会员折扣价展示（根据用户等级计算）

4. **等级列表页面**（可选）
   - 展示所有等级及升级条件
   - 高亮当前用户等级
