# 仓管端API测试指南

## 快速开始

### 1. 导入测试集合
1. 打开Postman
2. 点击Import按钮
3. 选择 `warehouse_api_test_collection.json` 文件
4. 导入完成

### 2. 配置环境变量
在Postman中设置以下环境变量：

| 变量名 | 值 | 说明 |
|--------|----|----|
| `base_url` | `https://zalonew.itaoth.com/index.php?s=` | API基础地址 |
| `wxapp_id` | `10001` | 小程序ID |
| `token` | (登录后自动设置) | 员工认证token |
| `shop_id` | `1` | 仓库ID |
| `package_id` | (测试时设置) | 包裹ID |
| `express_num` | (测试时设置) | 快递单号 |

### 3. 测试流程

#### 第一步：员工登录
```
POST /api/user/clerklogin
{
  "mobile": "员工手机号",
  "password": "密码",
  "wxapp_id": "10001"
}
```
登录成功后，token会自动保存到环境变量中。

#### 第二步：根据角色测试相应功能

## 角色权限测试

### 入库员 (角色ID: 1)
**测试顺序：**
1. 包裹入库校验 → 获取包裹信息
2. 包裹入库确认 → 完成入库操作
3. 包裹列表查询 → 查看入库包裹
4. 包裹详情查看 → 查看具体包裹信息
5. 包裹统计 → 查看统计数据

**关键测试点：**
- 扫描不存在的快递单号
- 重复入库同一包裹
- 权限验证（其他角色接口应该被拒绝）

### 分拣员 (角色ID: 2)
**测试顺序：**
1. 扫描货架码 → 获取货架信息
2. 包裹上架 → 将包裹分配到货架
3. 包裹移位 → 更换包裹货架位置
4. 包裹下架 → 从货架取下包裹
5. 拣货统计 → 查看拣货数据

**关键测试点：**
- 扫描不存在的货架码
- 上架状态错误的包裹
- 移动到不存在的货架位置

### 打包员 (角色ID: 3)
**测试顺序：**
1. 打包列表查询 → 查看待打包集运单
2. 扫码打包 → 扫描包裹进行打包
3. 集运单封箱 → 完成最终封箱
4. 打包统计 → 查看打包数据

**关键测试点：**
- 打包状态错误的包裹
- 封箱不完整的集运单
- 重复封箱操作

### 签收员 (角色ID: 4)
**测试顺序：**
1. 查询包裹 → 根据取货码查找包裹
2. 待取货列表 → 查看待签收包裹
3. 签收确认 → 确认用户签收
4. 确认取货 → 完成取货流程
5. 签收统计 → 查看签收数据

**关键测试点：**
- 查询不存在的取货码
- 签收状态错误的包裹
- 重复签收操作

### 海外入库员 (角色ID: 6)
**测试顺序：**
1. 海外包裹校验 → 验证海外包裹信息
2. 海外包裹入库 → 完成海外包裹入库

**关键测试点：**
- 处理特殊快递单号格式
- 海外包裹信息验证

## 常见测试场景

### 1. 正常业务流程测试
```
包裹预报 → 入库校验 → 入库确认 → 分拣上架 → 申请打包 → 
分拣下架 → 打包封箱 → 发货 → 签收
```

### 2. 异常情况测试
- **权限测试**：使用错误角色访问接口
- **状态测试**：在错误状态下执行操作
- **参数测试**：传递错误或缺失的参数
- **重复操作测试**：重复执行同一操作

### 3. 边界条件测试
- 空字符串参数
- 超长字符串参数
- 特殊字符处理
- 数值边界测试

## 测试数据准备

### 测试包裹数据
```json
{
  "express_num": "YT1234567890",
  "weight": "1.5",
  "length": "20",
  "width": "15", 
  "height": "10",
  "user_code": "USER001"
}
```

### 测试货架数据
```json
{
  "shelf_code": "SHELF001",
  "shelf_unit_id": "1"
}
```

### 测试用户数据
```json
{
  "mobile": "0123456789",
  "password": "123456",
  "user_id": "123"
}
```

## 响应格式说明

### 成功响应
```json
{
  "code": 1,
  "msg": "success",
  "data": {
    // 具体数据
  }
}
```

### 失败响应
```json
{
  "code": 0,
  "msg": "错误信息",
  "data": null
}
```

### 权限错误
```json
{
  "code": 0,
  "msg": "角色权限非法",
  "data": null
}
```

## 性能测试建议

### 1. 并发测试
- 多个员工同时操作同一包裹
- 高并发扫码操作
- 批量数据处理

### 2. 压力测试
- 大量包裹入库
- 长时间连续操作
- 数据库连接池测试

### 3. 稳定性测试
- 长时间运行测试
- 异常恢复测试
- 网络中断测试

## 常见问题排查

### 1. Token失效
- 重新登录获取新token
- 检查token格式是否正确
- 确认用户权限是否正常

### 2. 权限错误
- 确认员工角色配置
- 检查角色权限设置
- 验证员工账号状态

### 3. 包裹状态错误
- 检查包裹当前状态
- 确认操作顺序是否正确
- 查看包裹流转记录

### 4. 货架相关错误
- 确认货架码是否存在
- 检查货架容量限制
- 验证货架状态

## 测试报告模板

### 测试结果记录
| 接口 | 测试场景 | 预期结果 | 实际结果 | 状态 |
|------|----------|----------|----------|------|
| 员工登录 | 正确账号密码 | 登录成功 | 登录成功 | ✅ |
| 包裹入库 | 正常包裹 | 入库成功 | 入库成功 | ✅ |
| 权限验证 | 错误角色 | 权限拒绝 | 权限拒绝 | ✅ |

### 问题记录
| 问题描述 | 重现步骤 | 影响程度 | 状态 |
|----------|----------|----------|------|
| Token过期未提示 | 长时间操作后调用接口 | 中 | 待修复 |

## 自动化测试建议

### 1. 使用Postman Runner
- 创建测试套件
- 设置测试数据
- 配置断言验证

### 2. 集成CI/CD
- 定期执行回归测试
- 自动生成测试报告
- 异常情况告警

### 3. 数据驱动测试
- 准备测试数据集
- 批量执行测试用例
- 结果自动对比验证
