# 包裹性能优化测试指南

## 测试前准备

### 1. 部署数据库索引
```bash
# 连接数据库
mysql -u your_username -p your_database

# 执行索引创建脚本
source Lineminiapp/database_performance_indexes.sql

# 验证索引已创建
SHOW INDEX FROM package WHERE Table = 'package';
```

### 2. 清除浏览器缓存
- 打开浏览器开发者工具 (F12)
- 右键点击刷新按钮 → 选择"清空缓存并硬性重新加载"
- 或者在开发者工具的 Network 标签中勾选 "Disable cache"

### 3. 准备测试数据
确保数据库中有足够的测试数据：
- 至少 100+ 条包裹记录
- 包含不同状态的包裹
- 包含不同仓库的包裹

## 测试场景

### 场景 1: 首次加载性能测试

**目标**: 验证首次加载速度提升

**步骤**:
1. 清除浏览器缓存
2. 打开开发者工具 Network 标签
3. 访问包裹列表页面 `/order/package`
4. 记录 `package/outside` API 请求时间

**预期结果**:
- ✅ API 响应时间 < 1秒
- ✅ 只返回 20 条数据（第一页）
- ✅ 页面渲染流畅，无卡顿

**验证点**:
```javascript
// 检查返回数据结构
{
  "data": [...],           // 20条数据
  "total": 156,            // 总数
  "per_page": 20,          // 每页数量
  "current_page": 1,       // 当前页
  "last_page": 8           // 总页数
}
```

### 场景 2: Tab 切换性能测试

**目标**: 验证缓存机制有效性

**步骤**:
1. 在"已入库"标签页等待数据加载完成
2. 切换到"已发货"标签页
3. 再切换回"已入库"标签页
4. 观察 Network 标签

**预期结果**:
- ✅ 第一次切换到"已发货": 发起新的 API 请求
- ✅ 切换回"已入库": 不发起 API 请求（使用缓存）
- ✅ 切换速度 < 0.3秒

**验证点**:
- 检查 Network 标签，确认缓存命中时没有新请求
- 页面内容立即显示，无 loading 状态

### 场景 3: 搜索防抖测试

**目标**: 验证搜索防抖机制

**步骤**:
1. 在搜索框快速输入 "ABC123"
2. 观察 Network 标签的请求

**预期结果**:
- ✅ 输入过程中不会立即发起请求
- ✅ 停止输入 300ms 后才发起请求
- ✅ 只发起 1 次请求（不是 6 次）

**验证点**:
```javascript
// 检查请求参数
GET /package/outside?status=2&keyword=ABC123&page=1&page_size=20
```

### 场景 4: 分页加载测试

**目标**: 验证分页功能正常

**步骤**:
1. 访问包裹列表页面
2. 滚动到列表底部
3. 点击"โหลดเพิ่มเติม (1 / 8)"按钮
4. 观察数据加载

**预期结果**:
- ✅ 显示 loading 状态
- ✅ 加载第 2 页数据（20 条）
- ✅ 按钮文字更新为"โหลดเพิ่มเติม (2 / 8)"
- ✅ 底部显示"แสดง 40 จาก 156 รายการ"

**验证点**:
- 检查列表项数量增加
- 检查分页信息正确更新
- 检查 API 请求参数 `page=2`

### 场景 5: 手动刷新测试

**目标**: 验证刷新功能清除缓存

**步骤**:
1. 访问包裹列表页面，等待加载完成
2. 点击搜索框右侧的刷新按钮
3. 观察 Network 标签

**预期结果**:
- ✅ 发起新的 API 请求（忽略缓存）
- ✅ 统计数据也重新加载
- ✅ 页面显示最新数据

**验证点**:
- 检查 Network 标签有新的请求
- 检查请求参数包含 `forceRefresh=true`

### 场景 6: 统计缓存测试

**目标**: 验证统计数据缓存

**步骤**:
1. 访问包裹列表页面
2. 记录统计数据（已入库、已发货等数量）
3. 刷新页面（F5）
4. 观察 Network 标签

**预期结果**:
- ✅ 第一次访问: 调用 `package/countpack` API
- ✅ 5分钟内刷新: 不调用统计 API（使用缓存）
- ✅ 统计数据立即显示

**验证点**:
- 检查 Network 标签，确认缓存期内没有统计请求
- 统计数据显示正确

### 场景 7: 数据库索引验证

**目标**: 验证索引提升查询性能

**步骤**:
```sql
-- 执行查询分析
EXPLAIN SELECT * FROM package 
WHERE member_id = 1 
AND status = 2 
AND is_delete = 0 
ORDER BY created_time DESC 
LIMIT 20;
```

**预期结果**:
- ✅ `type` 列显示 `ref` 或 `range`（不是 `ALL`）
- ✅ `key` 列显示使用了索引（如 `idx_member_status_delete_time`）
- ✅ `rows` 列显示扫描行数较少

**验证点**:
```
+----+-------------+---------+------+--------------------------------+------+---------+-------+------+-------------+
| id | select_type | table   | type | possible_keys                  | key  | key_len | ref   | rows | Extra       |
+----+-------------+---------+------+--------------------------------+------+---------+-------+------+-------------+
|  1 | SIMPLE      | package | ref  | idx_member_status_delete_time  | ...  | ...     | const | 20   | Using where |
+----+-------------+---------+------+--------------------------------+------+---------+-------+------+-------------+
```

## 性能基准测试

### 使用浏览器开发者工具

1. **Network 标签**:
   - 记录 API 响应时间
   - 检查请求/响应大小
   - 验证缓存命中

2. **Performance 标签**:
   - 录制页面加载过程
   - 分析渲染性能
   - 检查内存使用

3. **Console 标签**:
   - 检查是否有错误
   - 验证日志输出

### 性能指标记录表

| 测试场景 | 优化前 | 优化后 | 提升 | 通过 |
|---------|--------|--------|------|------|
| 首次加载 | ___秒 | ___秒 | ___% | ☐ |
| Tab切换 | ___秒 | ___秒 | ___% | ☐ |
| 搜索响应 | ___秒 | ___秒 | ___% | ☐ |
| 分页加载 | ___秒 | ___秒 | ___% | ☐ |
| 数据量 | ___条 | ___条 | ___% | ☐ |

## 常见问题排查

### 问题 1: 分页数据不显示

**症状**: 点击"加载更多"后没有新数据

**排查**:
1. 检查 Network 标签，确认 API 请求成功
2. 检查返回数据结构是否正确
3. 检查 `pagination.hasMore` 状态

**解决**:
```javascript
// 检查 Console 是否有错误
console.log('Pagination:', pagination);
console.log('List length:', list.length);
```

### 问题 2: 缓存不生效

**症状**: 每次切换 Tab 都发起新请求

**排查**:
1. 检查缓存键是否正确生成
2. 检查 `forceRefresh` 参数
3. 清除浏览器缓存重试

**解决**:
```javascript
// 在 fetchOrderList 中添加日志
console.log('Cache key:', cacheKey);
console.log('Cache hit:', !!cache[cacheKey]);
```

### 问题 3: 搜索防抖不工作

**症状**: 输入时立即发起多个请求

**排查**:
1. 检查 `searchDebounceTimer` 是否正确设置
2. 检查定时器是否被清除
3. 检查 onChange 事件绑定

**解决**:
```javascript
// 检查定时器
console.log('Debounce timer:', searchDebounceTimer);
```

### 问题 4: 数据库索引未生效

**症状**: 查询仍然很慢

**排查**:
```sql
-- 检查索引是否存在
SHOW INDEX FROM package;

-- 检查查询是否使用索引
EXPLAIN SELECT * FROM package WHERE member_id = 1 AND status = 2;
```

**解决**:
- 重新执行索引创建脚本
- 检查表结构是否正确
- 运行 `ANALYZE TABLE package;` 更新统计信息

## 测试完成检查清单

- [ ] 首次加载速度 < 1秒
- [ ] Tab 切换使用缓存，速度 < 0.3秒
- [ ] 搜索防抖正常工作
- [ ] 分页加载功能正常
- [ ] 刷新按钮清除缓存
- [ ] 统计数据缓存 5 分钟
- [ ] 数据库索引已创建并生效
- [ ] 无 JavaScript 错误
- [ ] 无 API 错误
- [ ] UI 显示正常

## 测试报告模板

```
测试日期: ___________
测试人员: ___________
测试环境: ___________

性能测试结果:
- 首次加载: ___秒 (目标: <1秒) [通过/失败]
- Tab切换: ___秒 (目标: <0.3秒) [通过/失败]
- 搜索响应: ___秒 (目标: <0.5秒) [通过/失败]

功能测试结果:
- 分页加载: [通过/失败]
- 缓存机制: [通过/失败]
- 搜索防抖: [通过/失败]
- 手动刷新: [通过/失败]

问题记录:
1. ___________
2. ___________

总体评价: [优秀/良好/需改进]
```

## 联系支持

如有问题，请查看:
- 完整文档: `PACKAGE_PERFORMANCE_OPTIMIZATION_COMPLETE.md`
- 需求文档: `.kiro/specs/package-performance-optimization/requirements.md`
