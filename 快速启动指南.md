# 🚀 LINE Mini App 快速启动指南

## 📦 项目结构

```
D:\2025profile\
├── zalo_mini_app-master\     # 前端项目（LINE Mini App）
│   ├── src\                  # 源代码
│   ├── setup-local.bat       # 一键配置脚本
│   └── start-dev.bat         # 快速启动脚本
│
└── Lineminiapp\              # 后端项目（PHP + ThinkPHP）
    ├── web\                  # Web 根目录
    │   ├── index.php         # 入口文件
    │   └── cors-config.php   # CORS 配置（新建）
    └── source\               # 应用源码
```

---

## ⚡ 快速启动（3 步）

### 第 1 步：配置后端 CORS

编辑 `D:\2025profile\Lineminiapp\web\index.php`

在文件**最开头**添加（在 `<?php` 之后的第一行）：

```php
<?php
// 引入 CORS 配置（仅开发环境）
require_once __DIR__ . '/cors-config.php';

// ... 原有代码 ...
```

### 第 2 步：启动后端

打开命令提示符（CMD）：

```cmd
# 进入后端目录
cd D:\2025profile\Lineminiapp\web

# 启动 PHP 内置服务器
php -S localhost:8080
```

**保持此窗口运行！**

验证后端是否正常：
- 浏览器访问: `http://localhost:8080`
- 应该能看到网站首页

### 第 3 步：配置并启动前端

打开**新的**命令提示符窗口：

```cmd
# 进入前端目录
cd D:\2025profile\zalo_mini_app-master

# 运行一键配置脚本（首次运行）
setup-local.bat

# 或直接启动（如果已配置）
start-dev.bat
```

脚本会自动：
1. ✅ 配置 API 地址
2. ✅ 安装依赖
3. ✅ 启动开发服务器

---

## 🌐 访问应用

前端启动后，浏览器会自动打开：

```
http://localhost:5173
```

如果没有自动打开，手动访问上述地址。

---

## 📋 完整配置检查清单

### ✅ 后端配置

- [ ] PHP 已安装（版本 >= 7.2）
- [ ] MySQL 已安装并运行
- [ ] 数据库已导入
- [ ] `web/cors-config.php` 已创建
- [ ] `web/index.php` 已引入 CORS 配置
- [ ] 后端已启动在 `localhost:8080`
- [ ] 访问 `http://localhost:8080` 能看到页面

### ✅ 前端配置

- [ ] Node.js 已安装（版本 >= 16.x）
- [ ] npm 已安装（版本 >= 8.x）
- [ ] 已运行 `setup-local.bat` 配置脚本
- [ ] `src/config/config.js` 中 API 地址为 `http://localhost:8080`
- [ ] 依赖已安装（`node_modules` 目录存在）
- [ ] 前端已启动在 `localhost:5173`

### ✅ LINE 配置（可选，用于完整功能）

- [ ] 已在 LINE Developers Console 创建 LIFF App
- [ ] 已在商户后台配置 LINE 设置
- [ ] LIFF ID 已填写
- [ ] Channel ID/Secret 已填写
- [ ] Google Maps API Key 已填写

---

## 🔧 常见问题快速解决

### 问题 1: 前端无法连接后端

**错误信息**: `Network Error` 或 `CORS policy`

**解决方案**:
```cmd
# 1. 确认后端正在运行
# 访问 http://localhost:8080 应该能看到页面

# 2. 确认 CORS 配置已添加
# 检查 web/index.php 是否引入了 cors-config.php

# 3. 重启后端服务器
# 按 Ctrl+C 停止，然后重新运行
php -S localhost:8080
```

### 问题 2: 端口被占用

**错误信息**: `Port 8080 is already in use`

**解决方案**:
```cmd
# 查找占用端口的进程
netstat -ano | findstr :8080

# 结束进程（替换 <PID> 为实际进程 ID）
taskkill /PID <PID> /F

# 或使用其他端口
php -S localhost:8081

# 同时修改前端配置
# 编辑 src/config/config.js
# 将 8080 改为 8081
```

### 问题 3: npm install 失败

**错误信息**: 依赖安装出错

**解决方案**:
```cmd
# 清除缓存
npm cache clean --force

# 删除旧文件
rmdir /s /q node_modules
del package-lock.json

# 使用国内镜像重新安装
npm install --registry=https://registry.npmmirror.com
```

### 问题 4: 页面空白

**解决方案**:
```cmd
# 1. 打开浏览器开发者工具（F12）
# 2. 查看 Console 标签页的错误信息
# 3. 查看 Network 标签页的请求状态

# 4. 清除浏览器缓存
# 按 Ctrl+Shift+Delete

# 5. 重启前端服务器
# 按 Ctrl+C 停止，然后重新运行
npm run start
```

---

## 📱 测试功能

### 基础功能测试

1. **首页加载**
   - 访问 `http://localhost:5173`
   - 应该能看到首页内容

2. **API 连接测试**
   - 打开浏览器开发者工具（F12）
   - 查看 Network 标签
   - 应该能看到对 `localhost:8080` 的请求

3. **路由测试**
   - 点击导航菜单
   - 页面应该能正常切换

### 完整功能测试（需要 LINE 配置）

1. **LINE 登录**
   - 需要在 LINE 环境中测试
   - 本地开发可能无法完全模拟

2. **地图功能**
   - 需要配置 Google Maps API Key
   - 在地址管理页面测试

3. **支付功能**
   - 需要配置 LINE Pay
   - 在订单支付页面测试

---

## 🎯 开发工作流

### 日常开发流程

```cmd
# 1. 启动后端（终端 1）
cd D:\2025profile\Lineminiapp\web
php -S localhost:8080

# 2. 启动前端（终端 2）
cd D:\2025profile\zalo_mini_app-master
npm run start

# 3. 开始开发
# - 修改代码
# - 保存文件
# - 浏览器自动刷新（热更新）

# 4. 停止服务器
# 在各自终端按 Ctrl+C
```

### 代码修改后

- **前端代码**: 保存后自动刷新（热更新）
- **后端代码**: 需要重启 PHP 服务器
- **配置文件**: 需要重启对应服务器

---

## 📊 开发环境信息

| 项目 | 地址 | 说明 |
|------|------|------|
| 前端开发服务器 | http://localhost:5173 | Vite 开发服务器 |
| 后端 API | http://localhost:8080 | PHP 内置服务器 |
| 商户后台 | http://localhost:8080/store | 后台管理系统 |
| API 基础路径 | http://localhost:8080/index.php?s=api/ | RESTful API |

---

## 🔐 默认账号（如果需要）

### 商户后台登录

```
地址: http://localhost:8080/store
账号: admin
密码: [查看数据库或联系管理员]
```

---

## 📞 获取帮助

### 文档资源

- [本地开发配置指南.md](./zalo_mini_app-master/本地开发配置指南.md) - 详细配置说明
- [README.md](./zalo_mini_app-master/README.md) - 项目概述
- [DEPLOYMENT.md](./zalo_mini_app-master/DEPLOYMENT.md) - 部署指南
- [LINE_后台配置说明.md](./Lineminiapp/LINE_后台配置说明.md) - LINE 配置

### 技术支持

- **Email**: support@vhuongtra.com
- **文档**: 查看 `/openspec` 目录

---

## 🎉 成功标志

当看到以下内容时，说明配置成功：

### 后端成功
```
PHP 8.x Development Server (http://localhost:8080) started
```

### 前端成功
```
VITE v5.4.19  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
➜  press h + enter to show help
```

### 浏览器成功
- 页面正常显示
- 控制台无错误
- Network 标签显示 API 请求成功（状态码 200）

---

<div align="center">

**🚀 祝开发顺利！**

*LINE Mini App - Thailand Market*

</div>
