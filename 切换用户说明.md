# 切换测试用户说明

## 问题

打开 `http://localhost:3000` 时，仍然显示旧用户（31831）的数据，而不是新配置的用户15027。

## 原因

浏览器的localStorage中缓存了旧的token，即使修改了配置文件，浏览器仍然使用旧token。

## 解决方法

### 方法1：使用清理工具（最简单）⭐

1. 打开清理工具页面：
   ```
   http://localhost:3000/clear-token.html
   ```

2. 点击"清除Token并重新加载"按钮

3. 等待3秒，页面会自动跳转到首页并使用新用户登录

### 方法2：手动清除（快速）

1. 打开 `http://localhost:3000`

2. 按 `F12` 打开浏览器控制台

3. 在Console标签中执行：
   ```javascript
   localStorage.clear();
   window.location.reload();
   ```

4. 页面重新加载后会自动使用用户15027登录

### 方法3：重启开发服务器（彻底）

如果上面两个方法都不行：

1. 停止前端服务器（按 `Ctrl+C`）

2. 清除浏览器缓存：
   - Chrome: `Ctrl+Shift+Delete` → 选择"缓存的图片和文件" → 清除数据
   - 或者使用无痕模式：`Ctrl+Shift+N`

3. 重新启动服务器：
   ```bash
   npm run start
   ```

4. 打开 `http://localhost:3000`

## 验证是否成功

打开浏览器控制台（F12），应该看到：

```
🔧 Development mode: Auto-login enabled
🔑 Generating development token...
✅ Development token set successfully
   User: 海阔天空
   Token: a216e328616a783533040beb67f7d1a4
```

如果看到用户是"海阔天空"，说明切换成功！

## 查看当前用户

在控制台执行：

```javascript
console.log('Token:', localStorage.getItem('token'));
console.log('User ID:', localStorage.getItem('userId'));
```

应该显示：
- User ID: `15027`
- Token: `a216e328616a783533040beb67f7d1a4`（或其他新生成的token）

## 常见问题

### Q: 为什么清除后还是旧用户？

**A**: 可能是以下原因：
1. 浏览器缓存了页面 - 使用 `Ctrl+F5` 强制刷新
2. 开发服务器没有重启 - 重启 `npm run start`
3. 配置文件没有保存 - 检查 `src/config/config.js` 中 `userId: 15027`

### Q: 如何切换到其他测试用户？

**A**: 修改 `src/config/config.js`：

```javascript
export const DEV_AUTO_LOGIN = {
    enabled: appEnv === "development",
    userId: 31831, // 改为其他用户ID
    wxappId: 10001,
};
```

然后清除localStorage并重新加载。

### Q: 生产环境会受影响吗？

**A**: 不会！`DEV_AUTO_LOGIN.enabled` 在生产环境自动为 `false`，只在开发环境生效。

## 可用测试用户

- **15027 - 海阔天空** ⭐ 当前配置
- 31831 - 顺其自然
- 31823 - CS
- 31783 - lloxx
- 31782 - A快递快运.车队整车,调度
- 31778 - 亿霆国际物流-华华

## 快速命令

```bash
# 清除localStorage（在浏览器控制台）
localStorage.clear(); window.location.reload();

# 查看当前用户（在浏览器控制台）
console.log('User:', localStorage.getItem('userId'));

# 重启开发服务器（在终端）
# Ctrl+C 停止，然后：
npm run start
```

---

**提示**: 建议使用方法1的清理工具，最简单快捷！
