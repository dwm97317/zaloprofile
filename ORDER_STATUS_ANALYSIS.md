# 订单状态分析与前端更新

## 数据库状态定义 (yoshop_inpack)

### 当前数据库中使用的状态

| 状态码 | 状态名称 | 数据库记录数 | 说明 |
|--------|---------|-------------|------|
| -1 | 已取消/问题件 | 2条 | 订单被取消或有问题 |
| 1 | 待查验 | 14条 | 等待仓库查验 |
| 2 | 待支付 | 1条 | 等待用户支付 |
| 3 | 已支付 | 0条 | 用户已支付 |
| 4 | 已拣货 | 0条 | 仓库已拣货 |
| 5 | 已打包 | 0条 | 仓库已打包完成 |
| 6 | 已发货 | 0条 | 已发出国际物流 |
| 7 | 已收货 | 0条 | 用户已收到包裹 |
| 8 | 已完成 | 0条 | 订单完成 |

### 支付状态 (is_pay)

| 值 | 说明 | 记录数 |
|----|------|--------|
| 1 | 已支付 | 1条 |
| 2 | 未支付 | 16条 |

### 支付类型 (is_pay_type)

| 值 | 说明 | 记录数 |
|----|------|--------|
| 1 | 余额支付 | 16条 |
| 2 | 微信支付 | - |
| 3 | 支付宝 | - |
| 4 | 线下支付 | - |
| 5 | 现金支付 | 1条 |

## 前端当前状态映射

### Order/Index.jsx - Tab定义

```javascript
const tabs = [
  { id: "", label: "ทั้งหมด", icon: "📋" },      // 全部
  { id: 1, label: "ตรวจสอบ", icon: "⏱️" },      // 待查验 (verify)
  { id: 2, label: "ชำระ", icon: "💳" },         // 待支付 (nopay)
  { id: 3, label: "จัดส่ง", icon: "📦" },       // 待发货 (no_send)
  { id: 4, label: "ส่งแล้ว", icon: "🚚" },      // 已发货 (send)
  { id: 5, label: "เสร็จสิ้น", icon: "✅" },    // 已完成 (complete)
];
```

### EnhancedOrderListCard.jsx - 状态显示

当前前端只处理了部分状态:

```javascript
if (status === -1) return 'ยกเลิก';                    // 已取消
if (status === 1) return 'รอตรวจสอบ';                  // 待查验
if (status === 2 && is_pay === 2) return 'รอชำระเงิน'; // 待支付
if (status === 3 && is_pay === 1) return 'ชำระแล้ว';   // 已支付
if ((status === 4 || status === 5) && is_pay === 1) return 'กำลังแพ็ค'; // 打包中
if (status === 6 && is_pay === 1) return 'จัดส่งแล้ว';  // 已发货
if (status === 7 && is_pay === 1) return 'ได้รับแล้ว';  // 已收货
if (status === 8 && is_pay === 1) return 'เสร็จสิ้น';   // 已完成
```

## 问题分析

### 1. 缺失的状态

前端缺少以下状态的独立显示:
- **状态 4 (已拣货)** - 当前与状态5合并显示为"打包中"
- **状态 5 (已打包)** - 当前与状态4合并显示为"打包中"

### 2. Tab映射不准确

- Tab 3 (จัดส่ง/待发货) 映射到 `no_send`,但应该包含状态 3,4,5
- Tab 4 (ส่งแล้ว/已发货) 映射到 `send`,但应该包含状态 6,7

### 3. 状态颜色和图标

当前状态颜色配置:
- 状态 -1: 红色 ❌
- 状态 1: 灰色 ⏱️
- 状态 2: 橙色 💳
- 状态 3: 蓝色 ✅
- 状态 4-5: 紫色 📦
- 状态 6+: 绿色 🚚/✅

## 建议的改进方案

### 方案 1: 细化状态显示 (推荐)

在 `EnhancedOrderListCard.jsx` 中分别显示所有状态:

```javascript
if (status === -1) return 'ยกเลิก';                    // 已取消
if (status === 1) return 'รอตรวจสอบ';                  // 待查验
if (status === 2 && is_pay === 2) return 'รอชำระเงิน'; // 待支付
if (status === 3 && is_pay === 1) return 'ชำระแล้ว';   // 已支付
if (status === 4 && is_pay === 1) return 'กำลังคัดแยก'; // 拣货中 (NEW)
if (status === 5 && is_pay === 1) return 'แพ็คเสร็จแล้ว'; // 已打包 (NEW)
if (status === 6 && is_pay === 1) return 'จัดส่งแล้ว';  // 已发货
if (status === 7 && is_pay === 1) return 'ได้รับแล้ว';  // 已收货
if (status === 8 && is_pay === 1) return 'เสร็จสิ้น';   // 已完成
```

### 方案 2: 增加Tab (可选)

如果需要更细致的筛选,可以增加Tab:

```javascript
const tabs = [
  { id: "", label: "ทั้งหมด", icon: "📋" },           // 全部
  { id: 1, label: "ตรวจสอบ", icon: "⏱️" },           // 待查验
  { id: 2, label: "ชำระ", icon: "💳" },              // 待支付
  { id: 3, label: "ชำระแล้ว", icon: "✅" },          // 已支付 (NEW)
  { id: 4, label: "กำลังแพ็ค", icon: "📦" },         // 打包中 (NEW)
  { id: 5, label: "จัดส่ง", icon: "🚚" },            // 已发货
  { id: 6, label: "เสร็จสิ้น", icon: "✅" },         // 已完成
];
```

## 实施建议

1. **优先实施方案1** - 细化状态显示文本和颜色
2. **保持当前Tab结构** - 避免UI变化过大
3. **添加状态4和5的独立颜色** - 提升视觉区分度
4. **更新泰语翻译** - 确保状态名称准确

## 泰语状态翻译对照

| 状态码 | 中文 | 泰语 | 英文 |
|--------|------|------|------|
| -1 | 已取消 | ยกเลิก | Cancelled |
| 1 | 待查验 | รอตรวจสอบ | Pending Verification |
| 2 | 待支付 | รอชำระเงิน | Pending Payment |
| 3 | 已支付 | ชำระแล้ว | Paid |
| 4 | 已拣货 | กำลังคัดแยก | Picking |
| 5 | 已打包 | แพ็คเสร็จแล้ว | Packed |
| 6 | 已发货 | จัดส่งแล้ว | Shipped |
| 7 | 已收货 | ได้รับแล้ว | Received |
| 8 | 已完成 | เสร็จสิ้น | Completed |
