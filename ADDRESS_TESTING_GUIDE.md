# 📍 地址地图功能测试指南

## ✅ 实现状态: READY FOR TESTING

**日期**: 2026年1月15日  
**构建状态**: ✅ 成功 (5.45s, 990.94 KB / 289.35 KB gzipped)  
**代码检查**: ✅ 无错误  

---

## 🎯 测试目标

验证以下功能正常工作:
1. ✅ 交互式地图显示
2. ✅ 点击地图选择位置
3. ✅ 拖拽标记调整位置
4. ✅ 获取当前位置
5. ✅ 反向地理编码 (坐标→地址)
6. ✅ 地址搜索自动完成
7. ✅ 表单自动填充
8. ✅ 表单提交保存

---

## 🚀 快速开始

### 1. 启动开发服务器

```bash
cd D:\2025profile\zalo_mini_app-master
npm start
```

### 2. 访问测试页面

**增强版地址创建页面 (带地图)**:
```
https://localhost:9000/address/create-map
```

**原版地址创建页面 (不带地图)**:
```
https://localhost:9000/address/create
```

---

## 📋 测试用例

### 测试用例 1: 地图基本显示

**步骤**:
1. 访问 `https://localhost:9000/address/create-map`
2. 点击"เลือกจากแผนที่"按钮

**预期结果**:
- ✅ 地图平滑展开 (动画效果)
- ✅ 地图显示泰国曼谷区域 (默认位置)
- ✅ 地图上有一个红色标记 📍
- ✅ 地图可以拖拽、缩放
- ✅ 右上角显示"当前位置"按钮 🎯
- ✅ 地图下方显示当前坐标

**测试数据**:
- 默认坐标: `13.756300, 100.501800` (曼谷)

---

### 测试用例 2: 点击地图选择位置

**步骤**:
1. 展开地图
2. 在地图上任意位置点击

**预期结果**:
- ✅ 标记立即移动到点击位置
- ✅ 显示加载状态 (如果有)
- ✅ 坐标显示更新
- ✅ 表单字段自动填充:
  - 省份 (จังหวัด)
  - 区/县 (เขต/อำเภอ)
  - 街道/乡 (แขวง/ตำบล)
  - 邮编 (รหัสไปรษณีย์)
  - 详细地址 (บ้านเลขที่)

**测试位置建议**:
- 曼谷市中心: `13.7563, 100.5018`
- 素万那普机场: `13.6900, 100.7501`
- 芭提雅: `12.9236, 100.8825`

---

### 测试用例 3: 拖拽标记调整位置

**步骤**:
1. 展开地图
2. 点击并按住标记
3. 拖拽到新位置
4. 释放鼠标

**预期结果**:
- ✅ 标记跟随鼠标移动
- ✅ 地图中心跟随标记移动
- ✅ 释放后触发反向地理编码
- ✅ 坐标显示更新
- ✅ 表单字段自动更新

**注意事项**:
- 拖拽应该流畅无卡顿
- 地图应该自动居中到新位置

---

### 测试用例 4: 获取当前位置

**步骤**:
1. 展开地图
2. 点击右上角的"当前位置"按钮 🎯
3. 在浏览器弹窗中允许获取位置

**预期结果**:
- ✅ 浏览器请求位置权限
- ✅ 按钮显示加载动画
- ✅ 地图移动到当前位置
- ✅ 地图缩放级别调整到 16
- ✅ 标记移动到当前位置
- ✅ 自动反向地理编码
- ✅ 表单字段自动填充

**错误处理**:
- 如果用户拒绝权限: 显示错误提示
- 如果浏览器不支持: 显示不支持提示

---

### 测试用例 5: 地址搜索自动完成

**步骤**:
1. 在"🔍 ค้นหาที่อยู่"搜索框输入关键词
2. 从下拉列表选择地址

**预期结果**:
- ✅ 输入时显示建议列表
- ✅ 建议来自 Google Places API
- ✅ 选择后所有字段自动填充
- ✅ 坐标自动更新
- ✅ 如果地图已展开，标记移动到新位置

**测试关键词**:
- "สยามพารากอน" (Siam Paragon)
- "สนามบินสุวรรณภูมิ" (Suvarnabhumi Airport)
- "ถนนข้าวสาร" (Khao San Road)
- "เซ็นทรัลเวิลด์" (Central World)

---

### 测试用例 6: 表单验证

**步骤**:
1. 不填写任何字段
2. 点击"บันทึกที่อยู่"按钮

**预期结果**:
- ✅ 显示错误提示: "Please fill in required fields"
- ✅ 必填字段标记为红色 (*)
- ✅ 不提交表单

**必填字段**:
- ชื่อผู้รับ (姓名)
- เบอร์โทรศัพท์ (电话)
- จังหวัด (省份)

---

### 测试用例 7: 完整流程测试

**步骤**:
1. 访问页面
2. 填写联系人信息:
   - 姓名: "สมชาย ใจดี"
   - 电话: "0812345678"
3. 点击"เลือกจากแผนที่"展开地图
4. 点击地图选择位置 (或使用当前位置)
5. 确认地址信息自动填充
6. 可选: 修改地址详情
7. 填写海关信息 (可选):
   - ID Card: "1234567890123"
   - Clearance Code: "TXXXXXXX"
8. 点击"บันทึกที่อยู่"提交

**预期结果**:
- ✅ 所有步骤流畅无卡顿
- ✅ 地址自动填充准确
- ✅ 提交成功显示成功提示
- ✅ 返回上一页

---

## 🔍 详细功能检查

### 1. 地图交互

| 功能 | 检查项 | 状态 |
|------|--------|------|
| 地图显示 | 地图正常加载 | ⬜ |
| 地图拖拽 | 可以拖拽移动地图 | ⬜ |
| 地图缩放 | 可以缩放地图 | ⬜ |
| 标记显示 | 标记正常显示 | ⬜ |
| 标记拖拽 | 标记可以拖拽 | ⬜ |
| 点击地图 | 点击地图标记移动 | ⬜ |
| 当前位置 | 获取当前位置功能 | ⬜ |

### 2. 反向地理编码

| 功能 | 检查项 | 状态 |
|------|--------|------|
| API 调用 | 后端 API 正常响应 | ⬜ |
| 省份解析 | 省份字段正确填充 | ⬜ |
| 区县解析 | 区县字段正确填充 | ⬜ |
| 街道解析 | 街道字段正确填充 | ⬜ |
| 邮编解析 | 邮编字段正确填充 | ⬜ |
| 详细地址 | 详细地址正确填充 | ⬜ |
| 坐标保存 | 坐标正确保存 | ⬜ |

### 3. 地址搜索

| 功能 | 检查项 | 状态 |
|------|--------|------|
| 搜索框 | 搜索框正常显示 | ⬜ |
| 自动完成 | 输入时显示建议 | ⬜ |
| 选择地址 | 选择后自动填充 | ⬜ |
| 坐标更新 | 坐标正确更新 | ⬜ |
| 地图同步 | 地图标记同步移动 | ⬜ |

### 4. 表单功能

| 功能 | 检查项 | 状态 |
|------|--------|------|
| 字段输入 | 所有字段可以输入 | ⬜ |
| 字段验证 | 必填字段验证 | ⬜ |
| 自动填充 | 地址自动填充 | ⬜ |
| 手动修改 | 可以手动修改字段 | ⬜ |
| 提交保存 | 提交功能正常 | ⬜ |
| 错误处理 | 错误提示正常显示 | ⬜ |

### 5. UI/UX

| 功能 | 检查项 | 状态 |
|------|--------|------|
| 动画效果 | 地图展开/收起动画 | ⬜ |
| 加载状态 | 加载指示器显示 | ⬜ |
| 响应式 | 移动端适配 | ⬜ |
| 按钮状态 | 按钮禁用/启用 | ⬜ |
| 提示信息 | 使用说明显示 | ⬜ |
| 坐标显示 | 坐标实时更新 | ⬜ |

---

## 🐛 常见问题排查

### 问题 1: 地图不显示

**可能原因**:
- Google Maps SDK 未加载
- API Key 无效或过期
- 网络连接问题

**排查步骤**:
1. 打开浏览器控制台 (F12)
2. 检查是否有 JavaScript 错误
3. 检查网络请求是否成功
4. 验证 `window.google.maps` 是否存在

**解决方案**:
```javascript
// 在控制台检查
console.log(window.google);
console.log(window.google?.maps);
```

---

### 问题 2: 反向地理编码失败

**可能原因**:
- 后端 API 不可用
- 坐标超出泰国范围
- API 配额用尽

**排查步骤**:
1. 检查网络请求: `line_app/parse_address`
2. 查看响应状态码和数据
3. 验证坐标是否在泰国境内

**测试 API**:
```bash
# 使用 curl 测试后端 API
curl -X POST "https://your-backend.com/api/line_app/parse_address&wxapp_id=10001" \
  -H "Content-Type: application/json" \
  -d '{"lat": 13.7563, "lng": 100.5018}'
```

---

### 问题 3: 地址搜索无结果

**可能原因**:
- Google Places API 未启用
- 搜索关键词不准确
- 国家限制设置错误

**排查步骤**:
1. 检查 `AddressAutocomplete` 组件
2. 验证 `componentRestrictions: { country: "th" }`
3. 尝试不同的搜索关键词

---

### 问题 4: 当前位置获取失败

**可能原因**:
- 用户拒绝位置权限
- 浏览器不支持 Geolocation API
- HTTPS 连接问题

**排查步骤**:
1. 检查浏览器权限设置
2. 验证页面使用 HTTPS
3. 测试 `navigator.geolocation` 是否可用

**测试代码**:
```javascript
// 在控制台测试
if (navigator.geolocation) {
  navigator.geolocation.getCurrentPosition(
    (pos) => console.log('Success:', pos.coords),
    (err) => console.error('Error:', err)
  );
} else {
  console.error('Geolocation not supported');
}
```

---

### 问题 5: 表单提交失败

**可能原因**:
- 必填字段未填写
- 后端 API 错误
- 网络连接问题

**排查步骤**:
1. 检查表单验证逻辑
2. 查看网络请求: `address/add`
3. 验证请求数据格式

**调试代码**:
```javascript
// 在提交前打印数据
console.log('Form data:', form);
console.log('Payload:', payload);
```

---

## 📊 性能测试

### 1. 地图加载时间

**测试方法**:
1. 打开浏览器开发者工具
2. 切换到 Performance 标签
3. 点击"เลือกจากแผนที่"按钮
4. 停止录制并分析

**预期结果**:
- 地图初始化: < 1 秒
- 动画展开: 300ms
- 总加载时间: < 2 秒

---

### 2. 反向地理编码响应时间

**测试方法**:
1. 打开 Network 标签
2. 点击地图选择位置
3. 查看 `parse_address` 请求时间

**预期结果**:
- API 响应时间: < 500ms
- 表单更新时间: < 100ms
- 总响应时间: < 1 秒

---

### 3. 地址搜索响应时间

**测试方法**:
1. 在搜索框输入关键词
2. 观察建议列表出现时间

**预期结果**:
- 首次建议: < 500ms
- 后续建议: < 300ms
- 防抖延迟: 300ms

---

## 🔐 安全测试

### 1. API Key 保护

**检查项**:
- ✅ 前端代码不包含 API Key
- ✅ 反向地理编码通过后端代理
- ✅ 网络请求使用 HTTPS

**验证方法**:
```bash
# 搜索前端代码中的 API Key
grep -r "AIza" src/
# 应该没有结果
```

---

### 2. 输入验证

**检查项**:
- ✅ 坐标范围验证
- ✅ 必填字段验证
- ✅ 电话号码格式验证
- ✅ 邮编格式验证

**测试数据**:
- 无效坐标: `999, 999`
- 空字段: `""`
- 特殊字符: `<script>alert('xss')</script>`

---

### 3. 权限管理

**检查项**:
- ✅ 地理位置权限请求
- ✅ 权限拒绝处理
- ✅ 错误提示友好

---

## 📱 移动端测试

### 1. 响应式布局

**测试设备**:
- iPhone 12 Pro (390x844)
- iPad Pro (1024x1366)
- Samsung Galaxy S21 (360x800)

**检查项**:
- ✅ 地图高度适配
- ✅ 按钮大小适中
- ✅ 文字清晰可读
- ✅ 触摸区域足够大

---

### 2. 触摸交互

**检查项**:
- ✅ 地图可以触摸拖拽
- ✅ 标记可以触摸拖拽
- ✅ 按钮触摸响应
- ✅ 缩放手势支持

---

### 3. 性能优化

**检查项**:
- ✅ 地图加载流畅
- ✅ 动画不卡顿
- ✅ 内存占用合理
- ✅ 电池消耗正常

---

## ✅ 测试完成清单

### 基础功能
- [ ] 地图正常显示
- [ ] 点击地图选择位置
- [ ] 拖拽标记调整位置
- [ ] 获取当前位置
- [ ] 反向地理编码
- [ ] 地址搜索
- [ ] 表单自动填充
- [ ] 表单提交保存

### 高级功能
- [ ] 地图展开/收起动画
- [ ] 加载状态指示
- [ ] 错误处理
- [ ] 坐标实时显示
- [ ] 使用说明提示

### 兼容性
- [ ] Chrome 浏览器
- [ ] Safari 浏览器
- [ ] Firefox 浏览器
- [ ] Edge 浏览器
- [ ] 移动端浏览器

### 性能
- [ ] 地图加载速度
- [ ] API 响应速度
- [ ] 动画流畅度
- [ ] 内存占用

### 安全性
- [ ] API Key 保护
- [ ] 输入验证
- [ ] 权限管理
- [ ] HTTPS 连接

---

## 📝 测试报告模板

```markdown
# 地址地图功能测试报告

**测试日期**: YYYY-MM-DD  
**测试人员**: [姓名]  
**测试环境**: [浏览器/设备]  

## 测试结果

### 通过的测试
- ✅ [测试用例名称]
- ✅ [测试用例名称]

### 失败的测试
- ❌ [测试用例名称]
  - 错误描述: [详细描述]
  - 重现步骤: [步骤]
  - 预期结果: [描述]
  - 实际结果: [描述]

### 发现的问题
1. [问题描述]
   - 严重程度: [高/中/低]
   - 影响范围: [描述]
   - 建议修复: [描述]

## 总体评价
[整体评价和建议]
```

---

## 🎯 下一步行动

### 如果测试通过
1. ✅ 标记功能为"已完成"
2. ✅ 部署到测试环境
3. ✅ 通知相关人员测试
4. ✅ 收集用户反馈

### 如果测试失败
1. ❌ 记录问题详情
2. ❌ 分析根本原因
3. ❌ 修复问题
4. ❌ 重新测试

---

## 📞 支持联系

如果在测试过程中遇到问题:
1. 查看本文档的"常见问题排查"部分
2. 检查浏览器控制台错误信息
3. 查看网络请求详情
4. 联系开发团队

---

**测试状态**: 🟡 待测试  
**最后更新**: 2026年1月15日
