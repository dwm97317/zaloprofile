# 测试环境自动登录说明

## 功能说明

系统现在支持智能环境切换，**无需手动设置token**：

- ✅ **测试环境**：自动获取有效token
- ✅ **生产环境**：自动切换回LINE认证

## 使用方法

### 测试环境（本地开发）

```bash
npm run start
```

**就这么简单！** 系统会自动：
1. 检测到开发环境
2. 调用后端API生成token
3. 自动登录，可以直接使用所有功能

控制台会显示：
```
🔧 Development mode: Auto-login enabled
🔑 Generating development token...
✅ Development token set successfully
   User: 海阔天空
   Token: a216e328616a783533040beb67f7d1a4
```

### 生产环境（上线部署）

```bash
NODE_ENV=production npm run build
```

**无需修改任何代码！** 系统会自动：
1. 检测到生产环境
2. 禁用自动登录
3. 使用LINE LIFF认证

## 切换测试用户

如果需要使用不同的测试用户，修改 `src/config/config.js`：

```javascript
export const DEV_AUTO_LOGIN = {
    enabled: appEnv === "development",
    userId: 15027, // 测试用户ID
    wxappId: 10001,
};
```

可用测试用户：
- **15027 - 海阔天空** ⭐ 当前使用的测试用户
- 31831 - 顺其自然
- 31823 - CS  
- 31783 - lloxx
- 31782 - A快递快运.车队整车,调度
- 31778 - 亿霆国际物流-华华

## 技术实现

### 修改的文件

1. **src/config/config.js** - 添加自动登录配置
2. **src/utils/liff.js** - 实现自动登录逻辑

### 工作原理

```javascript
// 开发环境：自动调用后端API
GET /api/dev_token/generate?user_id=15027&wxapp_id=10001

// 生产环境：使用LINE认证
liff.init() → liff.login() → 获取真实用户token
```

## 安全说明

### ⚠️ 生产环境部署前

**必须删除**后端的DevToken控制器：
```bash
rm Lineminiapp/source/application/api/controller/DevToken.php
```

### ✅ 自动切换机制

- 开发环境：`DEV_AUTO_LOGIN.enabled = true`
- 生产环境：`DEV_AUTO_LOGIN.enabled = false`（自动设置）

系统会根据 `NODE_ENV` 环境变量自动切换，无需手动修改代码。

## 优势

✅ **零配置**：开发者无需手动设置token  
✅ **自动切换**：生产环境自动使用LINE认证  
✅ **提高效率**：即开即用，专注业务开发  
✅ **安全可靠**：生产环境完全禁用自动登录  

## 故障排除

### 问题：自动登录不工作

**解决方法**：
1. 检查后端是否运行：`http://localhost:8080`
2. 检查DevToken API：访问 `http://localhost:8080/index.php?s=api/dev_token/generate&user_id=15027&wxapp_id=10001`
3. 清除缓存：`localStorage.clear()` 然后刷新页面

### 问题：生产环境还在使用自动登录

**解决方法**：
1. 确保构建命令正确：`NODE_ENV=production npm run build`
2. 检查后端LINE配置：确保 `is_enable = true`

## 总结

现在你可以直接运行 `npm run start`，系统会自动处理登录，无需任何手动操作。上线时只需正常构建，系统会自动切换到LINE认证。

---

**更新时间**：2026年1月11日  
**状态**：✅ 已实现并测试
