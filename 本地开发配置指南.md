# 🚀 LINE Mini App 本地开发配置指南

## 📋 目录
- [前提条件](#前提条件)
- [快速开始](#快速开始)
- [手动配置](#手动配置)
- [后端配置](#后端配置)
- [前端配置](#前端配置)
- [启动项目](#启动项目)
- [常见问题](#常见问题)

---

## 前提条件

### 必需软件
- ✅ **Node.js** >= 16.x ([下载地址](https://nodejs.org/))
- ✅ **npm** >= 8.x (随 Node.js 安装)
- ✅ **PHP** >= 7.2 (后端运行环境)
- ✅ **MySQL** >= 5.7 (数据库)

### 检查安装
```bash
# 检查 Node.js 版本
node --version

# 检查 npm 版本
npm --version

# 检查 PHP 版本
php --version
```

---

## 快速开始

### 方法一：一键配置脚本（推荐）

#### Windows 用户
```cmd
# 在项目根目录运行
setup-local.bat
```

#### Mac/Linux 用户
```bash
# 添加执行权限
chmod +x setup-local.sh

# 运行配置脚本
./setup-local.sh
```

脚本会自动完成：
1. ✅ 配置后端 API 地址为 `http://localhost:8080`
2. ✅ 检查 Node.js 和 npm 环境
3. ✅ 安装所有依赖包
4. ✅ 提示是否启动开发服务器

### 方法二：手动配置

如果自动脚本失败，请按照下面的手动配置步骤操作。

---

## 手动配置

### 1. 后端配置

#### 1.1 启动后端服务器

确保后端已在 `localhost:8080` 运行：

```bash
# 进入后端目录
cd D:\2025profile\Lineminiapp\web

# 使用 PHP 内置服务器启动
php -S localhost:8080
```

或者使用其他方式（Apache/Nginx）配置虚拟主机到 `localhost:8080`。

#### 1.2 配置数据库

确保数据库已导入并配置正确：

```bash
# 检查数据库配置文件
D:\2025profile\Lineminiapp\source\application\database.php
```

#### 1.3 配置 LINE 设置

1. 登录商户后台：`http://localhost:8080/store`
2. 进入 **设置 → LINE 配置**
3. 填写以下信息：
   - **LIFF ID**: 从 LINE Developers Console 获取
   - **Channel ID**: LINE Login Channel ID
   - **Channel Secret**: LINE Login Channel Secret
   - **Google Maps API Key**: 从 Google Cloud Console 获取

#### 1.4 测试后端 API

在浏览器访问（注意使用小写和下划线）：
```
http://localhost:8080/index.php?s=api/line_app/base&wxapp_id=10001
```

或者使用完整路径：
```
http://localhost:8080/index.php?s=/api/line_app/base&wxapp_id=10001
```

应该返回类似以下的 JSON：
```json
{
  "code": 1,
  "data": {
    "config": {
      "is_enable": 1,
      "liff_id": "your-liff-id",
      "channel_id": "your-channel-id",
      "google_maps_key": "your-maps-key"
    }
  }
}
```

### 2. 前端配置

#### 2.1 安装依赖

```bash
# 进入前端项目目录
cd D:\2025profile\zalo_mini_app-master

# 安装依赖
npm install
```

#### 2.2 配置 API 地址

编辑 `src/config/config.js`：

```javascript
import { appEnv } from "./env";

// 开发环境配置 - 指向本地后端
const devBaseURL = "http://localhost:8080/index.php?s=api/";

// 生产环境配置
const proBaseURL = "https://zalonew.itaoth.com/index.php?s=api/";

export const BASE_URL = appEnv === "development" ? devBaseURL : proBaseURL;
export const TIMEOUT = 10000;
export const WXAPP_ID = "10001";
export const DEBUG = appEnv === "development";
```

#### 2.3 确认环境模式

检查 `src/config/env.js`：

```javascript
// 开发环境
export const appEnv = "development";

// 生产环境时改为
// export const appEnv = "production";
```

---

## 启动项目

### 1. 启动后端（如果还没启动）

```bash
cd D:\2025profile\Lineminiapp\web
php -S localhost:8080
```

保持此终端窗口运行。

### 2. 启动前端开发服务器

打开新的终端窗口：

```bash
cd D:\2025profile\zalo_mini_app-master
npm run start
```

### 3. 访问应用

前端开发服务器启动后，会自动打开浏览器访问：
```
http://localhost:5173
```

如果没有自动打开，手动在浏览器中访问上述地址。

---

## 开发环境架构

```
┌─────────────────────────────────────────┐
│  浏览器 (http://localhost:5173)         │
│  LINE Mini App 前端                      │
└─────────────────┬───────────────────────┘
                  │
                  │ HTTP 请求
                  │
┌─────────────────▼───────────────────────┐
│  后端 API (http://localhost:8080)       │
│  PHP + ThinkPHP 框架                     │
└─────────────────┬───────────────────────┘
                  │
                  │ SQL 查询
                  │
┌─────────────────▼───────────────────────┐
│  MySQL 数据库                            │
│  存储用户、订单、包裹等数据              │
└─────────────────────────────────────────┘
```

---

## 配置文件说明

### 前端配置文件

| 文件 | 说明 |
|------|------|
| `src/config/config.js` | API 地址、超时时间等核心配置 |
| `src/config/env.js` | 环境模式配置（development/production） |
| `src/utils/request.js` | Axios 请求拦截器配置 |
| `src/utils/liff.js` | LINE LIFF SDK 初始化配置 |
| `vite.config.js` | Vite 构建工具配置 |
| `package.json` | 项目依赖和脚本配置 |

### 后端配置文件

| 文件 | 说明 |
|------|------|
| `source/application/database.php` | 数据库连接配置 |
| `source/application/config.php` | 应用核心配置 |
| `source/application/store/extra/menus.php` | 后台菜单配置 |

---

## 常见问题

### Q1: 前端无法连接后端 API

**症状**: 浏览器控制台显示 CORS 错误或网络错误

**解决方案**:
1. 确认后端已启动在 `localhost:8080`
2. 检查 `src/config/config.js` 中的 `devBaseURL` 是否正确
3. 在后端添加 CORS 头部：

编辑 `Lineminiapp/web/index.php`，在文件开头添加：
```php
<?php
// 允许跨域请求（仅开发环境）
header('Access-Control-Allow-Origin: http://localhost:5173');
header('Access-Control-Allow-Methods: GET, POST, OPTIONS');
header('Access-Control-Allow-Headers: Content-Type, Authorization, platform, token');
header('Access-Control-Allow-Credentials: true');

// 处理 OPTIONS 预检请求
if ($_SERVER['REQUEST_METHOD'] === 'OPTIONS') {
    http_response_code(200);
    exit();
}
```

### Q2: npm install 失败

**症状**: 依赖安装过程中出错

**解决方案**:
```bash
# 清除 npm 缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

或使用国内镜像：
```bash
# 使用淘宝镜像
npm install --registry=https://registry.npmmirror.com
```

### Q3: 端口 5173 被占用

**症状**: 启动时提示端口已被使用

**解决方案**:
```bash
# Windows - 查找并结束占用端口的进程
netstat -ano | findstr :5173
taskkill /PID <进程ID> /F

# 或修改 vite.config.js 使用其他端口
# 在 server 配置中添加：
server: {
  port: 3000  // 改为其他端口
}
```

### Q4: LIFF SDK 初始化失败

**症状**: 控制台显示 "LIFF SDK initialization failed"

**解决方案**:
1. 确认后端 LINE 配置已正确填写
2. 检查 API 返回的 `liff_id` 是否正确
3. 本地开发时，LIFF 可能无法完全初始化（需要在 LINE 环境中测试）

**临时解决方案**（仅开发环境）:
在 `src/utils/liff.js` 中添加模拟数据：
```javascript
// 开发环境模拟 LIFF
if (process.env.NODE_ENV === 'development') {
  window.liff = {
    init: () => Promise.resolve(),
    isLoggedIn: () => true,
    getProfile: () => Promise.resolve({
      userId: 'dev-user-id',
      displayName: 'Dev User',
      pictureUrl: ''
    }),
    getIDToken: () => 'dev-token'
  };
}
```

### Q5: 页面显示空白

**症状**: 浏览器显示空白页面

**解决方案**:
1. 打开浏览器开发者工具（F12）查看控制台错误
2. 检查网络请求是否成功
3. 确认 `src/app.js` 中的路由配置正确
4. 清除浏览器缓存并刷新

### Q6: 热更新不工作

**症状**: 修改代码后页面不自动刷新

**解决方案**:
```bash
# 停止开发服务器（Ctrl+C）
# 重新启动
npm run start
```

或检查 `vite.config.js` 中的 HMR 配置。

---

## 开发工具推荐

### 浏览器扩展
- **React Developer Tools** - React 组件调试
- **Redux DevTools** - 状态管理调试（如果使用 Redux）
- **JSON Viewer** - 美化 API 响应

### VS Code 扩展
- **ES7+ React/Redux/React-Native snippets** - React 代码片段
- **Tailwind CSS IntelliSense** - Tailwind 自动补全
- **ESLint** - 代码检查
- **Prettier** - 代码格式化

---

## 调试技巧

### 1. 查看 API 请求

在 `src/utils/request.js` 中添加日志：

```javascript
// 请求拦截器
request.interceptors.request.use(config => {
  console.log('🚀 API Request:', config.url, config);
  return config;
});

// 响应拦截器
request.interceptors.response.use(response => {
  console.log('✅ API Response:', response.config.url, response.data);
  return response;
});
```

### 2. 查看 LIFF 状态

在浏览器控制台运行：
```javascript
// 检查 LIFF 是否已初始化
console.log('LIFF Ready:', window.liff?.isReady());

// 检查登录状态
console.log('Is Logged In:', window.liff?.isLoggedIn());

// 获取用户信息
window.liff?.getProfile().then(profile => {
  console.log('User Profile:', profile);
});
```

### 3. 查看 Recoil 状态

安装 Recoil DevTools：
```bash
npm install --save-dev recoil-devtools
```

在 `src/app.js` 中添加：
```javascript
import { RecoilDevTools } from 'recoil-devtools';

// 在 RecoilRoot 内添加
<RecoilRoot>
  <RecoilDevTools />
  {/* 其他组件 */}
</RecoilRoot>
```

---

## 生产环境部署

当开发完成后，需要部署到生产环境：

### 1. 修改环境配置

编辑 `src/config/env.js`：
```javascript
export const appEnv = "production";
```

### 2. 构建生产版本

```bash
npm run build
```

构建产物在 `dist/` 目录。

### 3. 部署到服务器

将 `dist/` 目录的内容上传到 Web 服务器。

详细部署步骤请参考 [DEPLOYMENT.md](./DEPLOYMENT.md)。

---

## 📞 技术支持

遇到问题？
1. 查看 [常见问题](#常见问题) 部分
2. 查看浏览器控制台错误信息
3. 查看后端 PHP 错误日志
4. 联系技术支持: support@vhuongtra.com

---

## 📚 相关文档

- [README.md](./README.md) - 项目概述
- [DEPLOYMENT.md](./DEPLOYMENT.md) - 部署指南
- [MIGRATION_COMPLETE.md](./MIGRATION_COMPLETE.md) - 迁移报告
- [openspec/project.md](./openspec/project.md) - 技术架构

---

<div align="center">

**本地开发配置指南 v1.0.0**

*最后更新: 2025-01-10*

</div>
