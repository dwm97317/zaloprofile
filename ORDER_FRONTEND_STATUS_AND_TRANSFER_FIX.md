# 前端订单列表状态和转单信息修复

## 修复日期
2026-01-15

## 修复内容

### 1. 修复"未知状态"问题 ✅

**问题**: 后台状态为"待发货"(status=3)的订单在前端显示为"未知状态"

**原因**: 前端状态映射逻辑不完整，缺少对 status=3 的处理

**修复**: 完善所有状态的映射逻辑

**文件**: `src/components/Order/EnhancedOrderListCard.jsx`

#### 修复前
```javascript
if (status === 3 && is_pay === 1) return 'ชำระแล้ว'; // 只处理已支付
// 缺少 status=3 的其他情况
```

#### 修复后
```javascript
if (status === 2 && is_pay === 2) return 'รอชำระเงิน'; // 待支付
if (status === 2 && is_pay === 1) return 'ชำระแล้ว'; // 已支付但状态还是2
if (status === 3) return 'รอจัดส่ง'; // 待发货 (已支付，等待发货)
if (status === 4) return 'กำลังคัดแยก'; // 拣货中
if (status === 5) return 'แพ็คเสร็จแล้ว'; // 已打包
if (status === 6) return 'จัดส่งแล้ว'; // 已发货
if (status === 7) return 'ได้รับแล้ว'; // 已收货
if (status === 8) return 'เสร็จสิ้น'; // 已完成
```

### 2. 添加转单信息高亮显示 ✅

**问题**: 已发货订单有转单信息时，前端没有显示转单承运商和转单单号

**修复**: 为已发货订单(status=6)添加转单信息显示区域

**文件**: `src/components/Order/EnhancedOrderListCard.jsx`

#### 显示逻辑
```javascript
// 1. 首先显示原始发货信息 (t_name, t_order_sn) - 青色背景
// 2. 如果有转单信息 (t2_order_sn 不为空)，显示转单信息 - 橙红色背景高亮
```

#### 视觉效果

**原始发货信息** (青色背景):
```
🚚 ขนส่ง: CJ专线
📮 หมายเลขติดตาม: 665456123
```

**转单信息** (橙红色背景，高亮显示):
```
🔄 ข้อมูลการโอนย้าย (转单信息)
🚚 ขนส่งใหม่: Flash Express (TH)  ← 红色字体
📮 หมายเลขติดตามใหม่: 654a6sd45612  ← 红色字体
```

### 3. 完善状态颜色配置 ✅

**修复**: 移除不必要的 `is_pay` 判断，简化状态配置逻辑

#### 修复前
```javascript
if (status === 3 && is_pay === 1) return { ... }; // 需要同时判断支付状态
if (status === 4 && is_pay === 1) return { ... };
```

#### 修复后
```javascript
if (status === 3) return { ... }; // 只判断订单状态
if (status === 4) return { ... };
// 状态本身已经隐含了支付状态，无需重复判断
```

## 完整状态映射表

| 状态码 | 中文 | 泰语 | 颜色 | 图标 |
|--------|------|------|------|------|
| -1 | 已取消 | ยกเลิก | 红色 | ❌ |
| 1 | 待查验 | รอตรวจสอบ | 灰色 | ⏱️ |
| 2 (未支付) | 待支付 | รอชำระเงิน | 橙色 | 💳 |
| 2 (已支付) | 已支付 | ชำระแล้ว | 蓝色 | ✅ |
| 3 | 待发货 | รอจัดส่ง | 蓝色 | 📦 |
| 4 | 已拣货 | กำลังคัดแยก | 靛蓝 | 🔍 |
| 5 | 已打包 | แพ็คเสร็จแล้ว | 紫色 | 📦 |
| 6 | 已发货 | จัดส่งแล้ว | 青色 | 🚚 |
| 7 | 已收货 | ได้รับแล้ว | 绿色 | 📬 |
| 8 | 已完成 | เสร็จสิ้น | 翠绿 | ✅ |

## 数据库字段说明

### 订单状态字段
- `status`: 订单状态 (-1 到 8)
- `is_pay`: 支付状态 (1=已支付, 2=未支付)

### 发货信息字段

**首次发货**:
- `t_number`: 承运商代码
- `t_name`: 承运商名称
- `t_order_sn`: 国际单号

**转单发货**:
- `t2_number`: 转单承运商代码
- `t2_name`: 转单承运商名称
- `t2_order_sn`: 转单国际单号
- `transfer`: 运输方式 (1=外部承运商, 0=自有物流)

## 显示规则

### 订单列表卡片

1. **所有订单**: 显示订单号、状态、基本信息
2. **已发货订单** (status=6):
   - 显示原始发货信息 (青色背景)
   - 如果有转单信息，显示转单信息 (橙红色背景高亮)
   - 两个区域都有独立的复制按钮

### 转单信息显示条件
```javascript
item.status === 6 && item.t2_order_sn
```

只有同时满足以下条件才显示转单信息:
1. 订单状态为已发货 (status=6)
2. 转单单号不为空 (t2_order_sn 存在)

## 测试验证

### 1. 测试状态显示

**步骤**:
1. 访问订单列表: `/order/index`
2. 查看不同状态的订单

**预期结果**:
- 所有状态都有对应的泰语文本
- 没有"ไม่ทราบสถานะ"(未知状态)
- 状态 3 显示为"รอจัดส่ง"(待发货)

### 2. 测试转单信息显示

**步骤**:
1. 访问订单列表
2. 找到已发货且有转单的订单 (status=6, t2_order_sn 不为空)

**预期结果**:
- 显示原始发货信息 (青色背景)
- 显示转单信息 (橙红色背景)
- 转单承运商和单号使用红色字体
- 两个区域都可以复制单号

### 3. 测试未转单订单

**步骤**:
1. 查看已发货但未转单的订单 (status=6, t2_order_sn 为空)

**预期结果**:
- 只显示原始发货信息
- 不显示转单信息区域

## 相关文件

| 文件 | 修改内容 |
|------|----------|
| `src/components/Order/EnhancedOrderListCard.jsx` | 完善状态映射、添加转单信息显示 |

## 技术细节

### 状态配置函数
```javascript
const getStatusConfig = () => {
  const { status, is_pay } = item;
  // 根据 status 返回对应的颜色、边框、图标配置
  // 简化逻辑，移除不必要的 is_pay 判断
}
```

### 状态文本函数
```javascript
const getStatusText = () => {
  const { status, is_pay } = item;
  // 返回泰语状态文本
  // 完整覆盖所有状态码 (-1 到 8)
}
```

### 转单信息组件
```jsx
{item.status === 6 && item.t2_order_sn && (
  <div className="p-3 bg-gradient-to-r from-orange-50 to-red-50 rounded-xl border-2 border-orange-300">
    {/* 转单承运商 */}
    {/* 转单单号 */}
    {/* 复制按钮 */}
  </div>
)}
```

## 注意事项

1. **向后兼容**: 修改保持向后兼容，未转单的订单不受影响
2. **视觉层次**: 转单信息使用更醒目的橙红色，便于用户识别
3. **用户体验**: 每个单号都有独立的复制按钮
4. **数据完整性**: 转单信息只在 t2_order_sn 存在时显示

## 泰语翻译对照

| 中文 | 泰语 | 用途 |
|------|------|------|
| 转单信息 | ข้อมูลการโอนย้าย | 转单区域标题 |
| 转单承运商 | ขนส่งใหม่ | 转单承运商标签 |
| 转单单号 | หมายเลขติดตามใหม่ | 转单单号标签 |
| 待发货 | รอจัดส่ง | 状态3文本 |
| 拣货中 | กำลังคัดแยก | 状态4文本 |
| 已打包 | แพ็คเสร็จแล้ว | 状态5文本 |

## 修复状态
✅ 已完成并测试

## 相关文档
- `ORDER_STATUS_ANALYSIS.md` - 订单状态分析
- `ORDER_SHIPPING_INFO_HIGHLIGHT.md` - 发货信息高亮显示
- `TRANSFER_DISPLAY_AND_NOTIFICATION_FIX.md` - 后台转单信息显示修复
