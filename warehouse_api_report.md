# 仓管端API深度分析报告

## 系统概述

仓管端是物流系统的核心操作模块，负责包裹的全生命周期管理，从入库到发货的所有环节。系统采用严格的角色权限控制，确保不同岗位员工只能执行相应的操作。

## 员工角色权限体系

### 角色定义
| 角色ID | 角色名称 | 权限描述 |
|--------|----------|----------|
| 1 | 入库员 | 负责包裹入库、验收、信息录入 |
| 2 | 分拣员 | 负责包裹上架、下架、货架管理 |
| 3 | 打包员 | 负责包裹打包、封箱、预发货 |
| 4 | 签收员 | 负责包裹签收、取货确认 |
| 5 | 仓管员 | 仓库管理、库存统计 |
| 6 | 海外入库员 | 海外仓库包裹入库 |

### 权限验证机制
- 每个API接口都有 `checkRole($role)` 权限验证
- 员工可以拥有多个角色权限（用逗号分隔存储）
- 权限验证失败返回"角色权限非法"错误

## 核心业务流程

### 1. 包裹入库流程
```
扫描包裹 → 验证包裹信息 → 录入重量尺寸 → 拍照存档 → 更新状态为"已入库"
```

### 2. 分拣上架流程
```
扫描包裹 → 选择货架位置 → 录入实际尺寸重量 → 上架 → 更新状态为"已拣货上架"
```

### 3. 打包发货流程
```
扫描集运单 → 验证包裹清单 → 打包封箱 → 生成预发货单 → 更新状态为"已打包"
```

### 4. 签收取货流程
```
扫描取货码 → 验证用户身份 → 确认签收 → 更新状态为"已收货"
```

## API接口详细分析

### 1. 入库员接口 (角色ID: 1)

#### 核心接口
| 接口 | 方法 | 功能 | 权限 |
|------|------|------|------|
| `/api/useropration/checkPack` | POST | 包裹入库校验 | 入库员 |
| `/api/useropration/inStoragePlus` | POST | 包裹入库确认 | 入库员 |
| `/api/useropration/instorage` | POST | 提交入库 | 入库员 |
| `/api/useropration/packlist` | POST | 包裹列表查询 | 入库员 |
| `/api/useropration/packdetails` | POST | 包裹详情查看 | 入库员 |

#### 入库校验接口详情
```json
{
  "接口": "/api/useropration/checkPack",
  "方法": "POST",
  "参数": {
    "code": ["快递单号"],
    "wxapp_id": "小程序ID"
  },
  "功能": "扫描快递单号，验证包裹信息",
  "返回": "包裹基本信息和状态"
}
```

### 2. 分拣员接口 (角色ID: 2)

#### 核心接口
| 接口 | 方法 | 功能 | 权限 |
|------|------|------|------|
| `/api/useropration/getShelf` | POST | 扫描货架码 | 分拣员 |
| `/api/useropration/checkUpShelf` | POST | 包裹上架 | 分拣员 |
| `/api/useropration/checkDownShelf` | POST | 包裹下架 | 分拣员 |
| `/api/useropration/packageMove` | POST | 包裹移位 | 分拣员 |
| `/api/useropration/loadingpackTotal` | POST | 拣货统计 | 分拣员 |

#### 包裹上架接口详情
```json
{
  "接口": "/api/useropration/checkUpShelf",
  "方法": "POST",
  "参数": {
    "id": ["包裹ID"],
    "shelf_id": ["货架单元ID"],
    "length": ["长度"],
    "width": ["宽度"],
    "height": ["高度"],
    "weight": ["重量"]
  },
  "功能": "将已入库包裹分拣上架到指定货架位置"
}
```

### 3. 打包员接口 (角色ID: 3)

#### 核心接口
| 接口 | 方法 | 功能 | 权限 |
|------|------|------|------|
| `/api/useropration/inpack` | POST | 打包列表查询 | 打包员 |
| `/api/useropration/createdOrder` | POST | 集运单封箱 | 打包员 |
| `/api/useropration/createdOrderByScan` | POST | 扫码打包 | 打包员 |
| `/api/useropration/inpackTotal` | POST | 打包统计 | 打包员 |

#### 集运单封箱接口详情
```json
{
  "接口": "/api/useropration/createdOrder",
  "方法": "POST",
  "参数": {
    "id": ["集运单ID"]
  },
  "功能": "对集运单进行最终打包封箱，生成预发货单"
}
```

### 4. 签收员接口 (角色ID: 4)

#### 核心接口
| 接口 | 方法 | 功能 | 权限 |
|------|------|------|------|
| `/api/useropration/searchPack` | POST | 查询包裹 | 签收员 |
| `/api/useropration/changeStatusByAdmin` | POST | 签收确认 | 签收员 |
| `/api/useropration/waitPickPack` | POST | 待取货列表 | 签收员 |
| `/api/useropration/appointmentToShop` | POST | 确认取货 | 签收员 |

### 5. 海外入库员接口 (角色ID: 6)

#### 核心接口
| 接口 | 方法 | 功能 | 权限 |
|------|------|------|------|
| `/api/useropration/checkzPack` | POST | 海外包裹校验 | 海外入库员 |
| `/api/useropration/inzyStoragePlus` | POST | 海外包裹入库 | 海外入库员 |

## 包裹状态流转

### 状态定义
| 状态码 | 状态名称 | 描述 |
|--------|----------|------|
| 1 | 未入库 | 包裹已预报但未到达仓库 |
| 2 | 已入库 | 包裹已到达仓库并完成入库 |
| 3 | 已拣货上架 | 包裹已分拣并上架到货架 |
| 4 | 待打包 | 包裹等待打包 |
| 5 | 待支付 | 集运单已生成，等待用户支付 |
| 6 | 已支付 | 用户已支付运费 |
| 7 | 已分拣下架 | 包裹已从货架下架准备打包 |
| 8 | 已打包 | 包裹已完成打包封箱 |
| 9 | 已发货 | 包裹已发出 |
| 10 | 已收货 | 用户已签收包裹 |
| 11 | 已完成 | 订单完成 |

### 状态流转图
```
预报(1) → 入库(2) → 上架(3) → 下架(7) → 打包(8) → 发货(9) → 收货(10) → 完成(11)
                ↓
            申请打包(4) → 支付(5) → 已支付(6)
```

## 数据统计接口

### 仓管端数据大屏
| 接口 | 功能 | 返回数据 |
|------|------|----------|
| `/api/useropration/screenData` | 仓库数据统计 | 各状态包裹数量统计 |
| `/api/useropration/packTotal` | 包裹总数统计 | 今日入库、总库存等 |
| `/api/useropration/inpackTotal` | 打包统计 | 待打包、已打包数量 |

## 货架管理系统

### 货架结构
- **货架(Shelf)** → **货架单元(ShelfUnit)** → **货架位置(ShelfUnitItem)**
- 每个包裹上架时会分配到具体的货架位置
- 支持货架码扫描快速定位

### 货架相关接口
| 接口 | 功能 |
|------|------|
| `/api/useropration/getShelf` | 扫描货架码获取货架信息 |
| `/api/useropration/updatetoshelf` | 批量包裹上架 |
| `/api/useropration/packageMove` | 包裹货架间移动 |

## 特殊功能

### 1. AI智能识别
- 支持百度OCR识别包裹信息
- 接口：`/api/apipost/reportpackToBaidu`
- 可自动识别快递单号、重量等信息

### 2. 消息通知系统
- 包裹状态变更自动发送微信模板消息
- 支持邮件通知功能
- 员工操作通知其他相关岗位

### 3. 物流轨迹记录
- 每个操作都会记录到物流轨迹表
- 用户可实时查看包裹状态变化
- 支持操作员信息记录

## 安全机制

### 1. 权限控制
- 严格的角色权限验证
- 操作员身份验证
- 仓库归属验证

### 2. 数据完整性
- 包裹状态流转验证
- 必要参数校验
- 数据库事务保护

### 3. 操作日志
- 完整的操作记录
- 异常情况追踪
- 责任人追溯

## 性能优化

### 1. 数据库优化
- 关键字段索引
- 分页查询
- 状态筛选优化

### 2. 缓存机制
- 员工信息缓存
- 货架信息缓存
- 统计数据缓存

### 3. 接口优化
- 批量操作支持
- 异步处理
- 错误重试机制
